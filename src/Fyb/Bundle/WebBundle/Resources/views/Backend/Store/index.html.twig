{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/Store:macros.html.twig' import list %}

{% block topbar %}
    <ol class="breadcrumb">
        <li>{{ 'sylius.breadcrumb.section.customer'|trans }}</li>
    </ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('fyb_backend_store_create'), 'fyb.store.create'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-user"></i> {{ 'fyb.store.index_header'|trans|raw }}</h1>
</div>

{{ pagination(stores) }}
{{ list(stores) }}
{{ pagination(stores) }}

{% endblock %}
